import { adder } from 'kalebr-frontend/helpers/adder'
import { module, test } from 'qunit'

module 'Unit | Helper | adder'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = adder 42
  assert.ok result
